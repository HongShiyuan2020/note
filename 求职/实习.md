## 分布式

1. CAP
2. BASE
3. Redis实现分布式锁
4. 分布式ID（情况、UUID、数据库段号、Redis、雪花算法）
5. 分布式事务（2PC、3PC、TCC、SAGA、消息队列、本地消息表、AT）
6. RPC（动态代理、序列化、反序列化、服务发现）
7. 限流算法（固定窗口、滑动窗口、漏桶、令牌桶）
8. 分布式一致性算法（Raft【领导选举、日志复制】、Paxos【】）

## 消息队列
1. 消息队列的优缺点（解耦、异步、削峰；可用性、复杂性、一致性）
2. 常用消息队列的对比（Kafka【吞吐量；数据丢失、功能单一、Offset维护】、RabbitMQ、RocketMQ【延迟队列、消息事务；吞吐量比Kafka稍弱】）
3. 重复消费（唯一键、消息ID+Redis/Mysql消息表、乐观锁、分布式锁、状态机）
4. 消息丢失（ACK机制+MQ集群+手动ACK）
5. 可靠性与顺序性（持久化、确认、重试；）
6. 消息队列扩容
7. 延迟消息队列
8. Kafka为什么吞吐量巨大（顺序写入、批处理、零拷贝、压缩）
9. RabbitMQ的组件（生产者、消费者、交换机、队列、绑定）
10. RabbitMQ的交换机类型（Direct、Fanout、Topic、Headers）
11. RabbitMQ的优点（持久化、消息确认、镜像队列、交换器类型）
12. RabbitMQ的延迟队列和死信队列

## MySQL
1. SQL查询的执行顺序（FROM、ON、JOIN、WHERE、GROUP、HAVING、SELECT、ORDER、LIMIT）
2. SQL的执行过程（连接、查缓存、解析、执行（预处理、优化、执行））
3. Mysql引擎（InnoDB、MyISAM、Memory）
4. 索引的分类（B+Tree索引、Hash索引、Full-Text索引；聚簇索引、二级索引；单列索引、联合索引；主键索引、唯一索引、普通索引、前缀索引）
5. B+树的叶子节点如何查询（页目录配合二分）
6. B+树和B树的对比
7. 联合索引（最左匹配）
8. 索引失效的情况（LIKE左模糊或左右模糊、WHERE中使用函数、对索引列进行表达式计算、数字和字符串比较会添加CAST把字符串转换为数字再比较、联合索引不满足最左匹配、WHERE中OR前是索引OR后不是索引）
9. 索引优化（前缀索引、覆盖索引、主键自增、防止索引失效）
10. 事务的特性（ACID：原子性、一致性、隔离性、持久性）
11. 并发问题（脏读、不可重复读、幻读）
12. 解决并发问题的方法（锁、事务隔离级别、MVCC多版本并发控制）
13. 事务隔离级别（读未提交（脏读）、读已提交（不可重复读）、可重复读（默认；幻读）、串行）
14. MVCC（ReadView、ReadView的四个字段（本事务ID、活动事务IDs、创建时最小的事务ID、创建时要分配给下一个事务的ID）、记录的额外字段（上一个修改该记录的事务ID、Undo日志指针））
15. 锁的分类（全局锁、表级锁（表锁、元数据锁、意向锁、AUTO-INC锁）、行级锁（Record Lock、Gap Lock、Next-Key Lock））
16. 日志的分类（Redo Log（持久性）、Undo Log（原子性）、Bin Log（数据备份和主从复制）、慢查询日志）
17. BinLog的两阶段提交
18. MySQL两次写
19. MYSQL性能调优过程（定位开启慢查询日志、EXPLAIN分析慢查询的SQL语句、优化查询、分表分库利用缓存）
20. MySQL的主从复制
21. 分库分表（垂直、水平）

## Redis
1. Redis的数据结构（String、Hash、Set、ZSet、List）
2. 其他结构（HyperLogLog、BitMap、Geo、Stream）
3. ZSet的底层实现和应用（跳表；排行榜、延迟任务队列、范围查询）
4. Hash的扩容（两个Hash、渐进式rehash）
5. String的底层实现（SDS【len、alloc、flags、buf[]】）
6. List的底层实现（压缩列表【zlbytes、zltail、zllen；zlend；prevlen、encoding、data】、listpack【encoding、data、len】）
7. Redis的线程模型（网络IO【基于epoll的多路复用、6.0之后多线程】+命令解析+执行命令：单线程、关闭文件、AOF刷盘、Lazy Free释放内存）
8. 原子性（单条指令、Lua脚本、Redis事务（无回滚））
9. 持久性（RDB、AOF刷盘、两者混合）
10. AOF策略（Always、EverySec、No）
11. 缓存淘汰和过期删除（内存满进行淘汰；对已经过期的数据进行删除）
12. 缓存淘汰策略（不进行淘汰；设置了过期【ltt】；未设置过期；【随机、lrn、lfu】）
13. 过期删除策略（惰性删除+定期删除【25%删除策略】）
14. 增量同步和完全同步（环形缓冲；RDB同步+同步时AOF写命令）
15. Redis哨兵与选主方法（主观下线、客观下线；自主竞选Sentinel Leader；Leader确定新主节点【过滤故障、选择优先级最高、复制偏移量最高、runid最小】）
16. 集群模式（主从；切片集群【16384个hash槽】）
17. Redis的应用（缓存、排行榜、消息队列、分布式锁、计数器）
18. 大Key和热Key问题
19. Redis缓存方案（旁路方案、消息队列方案、订阅Binlog再删除缓存）
20. 缓存雪崩、缓存击穿、缓存穿透（互斥锁、过期时间、后台更新；非法请求限制、缓存空值、布隆过滤器）
21. 防超卖（排他锁（悲观锁）、乐观锁、分布式锁、分布式锁+分段、redis的原子性+异步队列）

## JVM
1. JVM内存分区（方法区（元空间）、虚拟机栈（局部变量表、操作数栈、动态链接、方法出口）、本地方法栈、程序计数器、堆（新生代、老年代）、直接内存）
2. 方法区的方法执行过程（解析方法调用、创建栈帧、执行、返回）
3. 方法区的细分（类信息、常量池、静态变量、编译后的字节码、符号引用、运行时常量池）
4. String的存放位置（字面值在常量池、new关键字在堆中）
5. 引用类型（强、软、弱、虚）
6. 内存泄漏与内存溢出（静态属性、未关闭资源、ThreadLocal）
7. 类加载过程（启动、扩展、应用程序、自定义；双亲委派【保证类的唯一性和安全性、实现类的复用】；加载、验证、准备、解析、初始化、使用、卸载）
8. 出发垃圾回收的时机（内存不足、手动gc、JVM参数-Xmx -Xms、对象数量或内存达到阈值）
9. 引用计数法和可达性分析（GC Roots【虚拟机栈中引用、本地方法栈、静态属性、方法去常量】）
10. CG算法（标记-清除、复制、标记整理）
11. 垃圾回收器（G1（均衡）、CMS（标记清除、STW短）、ZGC（极低延迟）、Parallel Scavenge（复制新生代） + Parallel Old（标记-整理））
12. Minor GC【Eden不足】、Major GC【老年代空间不足、新生代晋升过快】、 Full GC【手动gc、新生代晋升过快老年代放不下、元空间空间不足】
## Spring
1. IoC和AOP
2. 依赖注入的方法（构造器注入、Setter方法注入、字段注入）
3. 循环依赖的解决方法（三级缓存）
4. Spring事务失效的情况（未捕获异常、非受检异常、事务传播不当、多数据源事务管理、跨方法调用事务、非公开方法、This调用）
5. Bean的生命周期（扫描加载BEAN配置、实例化、注入依赖、三个Aware、BeanPostProcessor、InitializingBean、DisposableBean）
6. Spring MVC处理流程（DispatchServlet、HandlerMapping、HanderAdpter）
7. Spring Boot约定大于配置（自动化配置、默认配置、项目结构）
8. Spring Boot自动装配（@EnableAutoConfiguration；扫描类路径上的META-INF/spring.factories、查找实现了Auto Configuration的类、根据条件判断机制判断是否满足导入条件）
9. 过滤器和拦截器
10. MyBatis中的#和$
11. Spring Cloud

## 项目
1. 异步回写的问题
	1. **数据丢失风险**： 
		1. 缓存节点崩溃、消息队列崩溃、回写进程崩溃
		2. 先写Redis、再放消息队列
	2. **数据一致性**：延迟导致的不一致性、并发冲突（针对同一个Key的大量操作的顺序性问题）
	3. 运维复杂与排查：
	4. 数据库写入压力瞬间爆发
	5. 级联失效
2. 消息队列的重试策略：
	1. 根据业务进行重试：常规业务3-5次、关键业务10-15次、超过最大次数投递到死信队列通知用户
	2. 指数退避策略：
		1. 第一次：10s
		2. 第二次：30s

## 自我介绍

面试官您好，我是洪世渊。目前是一名就读于南京师范大学计算及科学与技术专业的研究生。在专业技能上我以Java与Pyhton为主要开发语言，对JVM、并发编程、数据库原理等方面有比较深入的了解。此前，我主导或参与过多个差异化项目：包括中学物理实验引导与评估系统、中文人名识别系统以及一个微服务架构下的博客系统的开发。其中我主要负责一些算法模块和核心功能的实现与部署工作，积累了一些项目经验。希望能有机会加入贵公司，在贡献技术价值的同时，与团队共同成长。谢谢。

## 智能体
1. 定义：智能体被定义为任何能够通过**传感器（Sensors）**感知其所处**环境（Environment）**，并**自主**地通过**执行器（Actuators）**采取**行动（Action）**以达成特定目标的实体。
2. 演进：
	1. 依赖于人类设计师的先验知识
		1. 简单反射智能体（Simple Reflex Agent）
		2. 基于模型的反射智能体（Model-Based Reflex Agent）
		3. 基于目标的智能体（Goal-Based Agent）
		4. 基于效用的智能体（Utility-Based Agent）
	2. 学习型智能体（Learning Agent）
	3. LLM驱动的智能体
3. 类型：
	1. **基于内部决策架构的分类**
	2. **基于时间与反应性的分类**
		1. 反应式智能体 (Reactive Agents)
		2. 规划式智能体(Deliberative Agents)
		3. 混合式智能体(Hybrid Agents)
	3. **基于知识表示的分类**
		1. 符号主义 AI（Symbolic AI）
		2. 亚符号主义 AI（Sub-symbolic AI）
		3. 神经符号主义 AI（Neuro-Symbolic AI）
4. 协作模式
	1. 作为开发者工具的智能体
	2. 作为自主协作者的智能体
		1. **单智能体自主循环**
		2. **多智能体协作**
		3. **高级控制流架构**
5. PEAS
	1. 性能度量
	2. 环境
	3. 执行器
	4. 传感器
6. 架构范式
	1. ReAct：一种将“思考”和“行动”紧密结合的范式，让智能体边想边做，动态调整步走一步看一步
		1. 形式化：$(th_{t},a_{t})=\pi(q,(a_1,o_1),...,(a_{t−1},o_{t−1})) \quad o_t =T(a_t)$
		2. 优点：
			1. **高可解释性**
			2. **动态规划与纠错能力**
			3. **工具协同能力**
		3. 缺点：
			1. **对LLM自身能力的强依赖**
			2. **执行效率问题**
			3. **提示词的脆弱性**
			4. **可能陷入局部最优**
	2. **Plan-and-Solve：** 一种“三思而后行”的范式，智能体首先生成一个完整的行动计划，然后严格执行。**先规划 (Plan)，后执行 (Solve)**
		1. 形式化：
			1. 规划阶段：$P=(p_1,p_2,...,p_n) \quad P=\pi_{plan}(q)$
			2. 执行阶段：$s_i=\pi_{solve}(q, P, (s_1, s_2, ..., s_{i-1}))$
	3. **Reflection：** 一种赋予智能体“反思”能力的范式，通过自我批判和修正来优化结果。**事后（post-hoc）的自我校正循环**
		1. 形式化：
			1. 执行：$O_0$
			2. 反思：$F_i=\pi_{reflect}(\text{Task}, O_i)$
			3. 优化：$O_{i+1}=\pi_{refine}(\text{Task}, O_i, F_i)$
	4. 
	5. 

## Transformer
1. FlashAttention
2. Transformer-Mamba
3. Mixture of Experts
4. **RoPE (Rotary Position Embedding)**
5. GQA 与 KV Cache 压缩

## 大模型
1. 提示工程可以调整哪些
	1. **模型采样参数**（Top-k、Temputer、Top-p）
	2. （零、单、少）样本
	3. 指令调优
	4. 角色扮演
	5. 示例
	6. 思维链
2. 大模型幻觉
	1. 定义：大语言模型生成的内容与客观事实、用户输入或上下文信息相矛盾，或者生成了不存在的事实、实体或事件。
	2. 成因：
		1. 训练数据中可能包含错误或矛盾的信息
		2. 模型的自回归生成机制决定了它只是在预测下一个最可能的词元，而没有内置的事实核查模块。
		3. 面对需要复杂推理的任务时，模型可能会在逻辑链条中出错，从而“编造”出错误的结论
		4. 知识时效性不足和训练数据中存在的偏见
	3. 缓解方法：
		1. 通过高质量数据清洗、引入事实性知识以及强化学习与人类反馈 (RLHF) 等方式[13]，从源头减少幻觉。
		2. 探索新的模型架构，或让模型能够表达其对生成内容的不确定性。
		3. **检索增强生成 (Retrieval-Augmented Generation, RAG)**
		4. **多步推理与验证**： 引导模型进行多步推理，并在每一步进行自我检查或外部验证。
		5. **引入外部工具**： 允许模型调用外部工具（如搜索引擎、计算器、代码解释器）来获取实时信息或进行精确计算。
	
## Java基础
1. Java对象创建过程
2. JMM
3. 

## 简历

1. Write-Behind + MQ 缓存点赞量、访问量、聊天室在线状态等信息
2. Cache-Aside + 本地消息表 + 状态字段 + OSS 存储文章内容
3. 